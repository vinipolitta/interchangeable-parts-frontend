// src/environments/environment.prod.ts
// Variáveis de ambiente para o modo de produção.
export const environment = {
    production: true, // Indica que é ambiente de produção
    apiUrl: 'https://api.seudominio.com/api/v1' // URL base da API do backend em produção
  };