// Variáveis de ambiente para o modo de desenvolvimento.
export const environment = {
    production: false, // Indica que não é ambiente de produção
    apiUrl: 'http://localhost:8080/api/v1' // URL base da API do backend em desenvolvimento
  };